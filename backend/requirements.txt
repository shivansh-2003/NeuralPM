# --- Core stack (pinned to avoid the version-drift failures the research flagged) ---

# Memory abstraction — pinned to 1.0.11, NOT >=1.0.0. mem0-falkordb 0.4.1
# (the only published version) monkey-patches mem0.utils.factory.GraphStoreFactory
# and mem0.graphs.configs.GraphStoreConfig — both were removed in mem0ai 2.x's
# restructure, so anything >=2.0.0 breaks register() at import time.
# No [graph] extra: it pulls in kuzu, which has no prebuilt wheel and fails to
# build from source on newer Pythons. We don't need it — FalkorDB is wired in
# via mem0-falkordb's own patch, not mem0's built-in graph extras.
mem0ai==1.0.11

# FalkorDB graph memory plugin for mem0.
# register() patches mem0 internals at runtime — no fork of mem0 needed.
# Blog: https://www.falkordb.com/blog/graph-memory-llm-agents-mem0-falkordb/
# Repo:  https://github.com/FalkorDB/mem0-falkordb
mem0-falkordb==0.4.1

# FalkorDB Python client (Redis wire protocol — same port 6379)
falkordb>=1.0.0

# Vector store client. Keep within 1 minor of your running Qdrant server image.
qdrant-client==1.12.1

# Agent orchestration. Pin langgraph and langgraph-prebuilt to matching lines.
langgraph==0.2.60

# Ollama chat + embedding integration (use langchain-ollama, NOT langchain_community)
langchain-ollama==0.2.2
langchain-core==0.3.40



# API layer
fastapi==0.115.6
uvicorn[standard]==0.34.0
websockets>=12.0          # WebSocket support for FastAPI (used from I-5 onward)

# Postgres driver (used by db.py and store_node from I-1 onward)
psycopg2-binary>=2.9

# HTTP client (used by synthesize_node for Qwen2.5-VL calls in I-9)
httpx>=0.27.0

# Task queue (used from I-2 onward for decay + risk scan jobs)
celery>=5.0
redis>=5.0

# Misc
python-dotenv==1.0.1

# --- domain/ layer (SQLAlchemy ORM + Alembic, separate from the psycopg2/schema.sql
# stack above used by memory_agent — see main.py vs api.py) ---
SQLAlchemy==2.0.50
alembic==1.19.1
pydantic-settings==2.15.0
pytest==9.1.1

# --- Phase 0.5: auth + realtime (non-AI infra) ---
passlib[bcrypt]
# pinned: passlib 1.7.4 (unmaintained) probes bcrypt.__about__, removed in bcrypt>=4.1
bcrypt==4.0.1
python-jose[cryptography]
email-validator
